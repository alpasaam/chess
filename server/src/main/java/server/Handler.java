package server;

public class Handler {
    //this is where you can have a toJson method that takes in a list of objects and returns a json string and
    // a fromJson method that takes in a json string and returns a list of objects

    // you can also authenticate users here

}
